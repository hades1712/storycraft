# ===========================================
# NextAuth.js 配置
# ===========================================
# NextAuth.js 需要这个密钥来加密 JWT tokens 和 session cookies
# 在生产环境中这是必需的，请使用强密码
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=(openssl rand -base64 32)

# ===========================================
# Google OAuth 配置
# ===========================================
# 从 Google Cloud Console 获取这些值
# https://console.cloud.google.com/apis/credentials
#AUTH_GOOGLE_ID=your-google-client-id.apps.googleusercontent.com
#AUTH_GOOGLE_SECRET=your-google-client-secret
GOOGLE_APPLICATION_CREDENTIALS=./path/to/your/service-account-file.json
# ===========================================
# Google Cloud Platform 配置
# ===========================================
# 您的 GCP 项目配置
PROJECT_ID=Project_ID
LOCATION=location
FIRESTORE_DATABASE_ID=(default)

# Google Cloud Storage 配置
# 统一的 GCS 存储桶名称，用于存储所有类型的文件（视频、音频、图片等）
# 不同文件类型通过子目录区分：videos/, audio/, images/
GCS_BUCKET_NAME=bucketname
# ===========================================
# 应用配置
# ===========================================
# 日志级别
LOG_LEVEL=info

# 是否使用 Cosmo（视频生成相关）
USE_COSMO=false

# 用户注册密钥（用于验证用户是否有权限注册）
APP_SECRET_KEY=(openssl rand -base64 12)

# ===========================================
# 开发环境配置
# ===========================================
NODE_ENV=development