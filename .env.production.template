# =============================================================================
# 生产环境配置模板 - StoryCraft 应用
# =============================================================================
# 使用说明：
# 1. 复制此文件为 .env.production: cp .env.production.template .env.production
# 2. 填入实际的生产环境值
# 3. 确保不要将 .env.production 提交到版本控制系统
# =============================================================================

# -----------------------------------------------------------------------------
# NextAuth.js 认证配置
# -----------------------------------------------------------------------------
# NEXTAUTH_URL: 应用的完整 URL，用于 OAuth 回调和会话管理
# 格式: https://your-service-name-HASH-REGION.a.run.app
# 注意: 首次部署后需要更新为 Cloud Run 生成的实际 URL
NEXTAUTH_URL=https://your-app-url.run.app

# NEXTAUTH_SECRET: NextAuth.js 用于加密 JWT 和会话的密钥
# 生成命令: openssl rand -base64 32
# 重要: 生产环境必须使用强随机密钥，不要使用示例值
NEXTAUTH_SECRET=your-production-secret-key

# -----------------------------------------------------------------------------
# Google OAuth 2.0 配置 (可选，用于 Google 登录)
# -----------------------------------------------------------------------------
# 获取方式: Google Cloud Console > APIs & Services > Credentials
# 1. 创建 OAuth 2.0 客户端 ID
# 2. 设置授权重定向 URI: https://your-domain/api/auth/callback/google
# 3. 复制客户端 ID 和密钥
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# -----------------------------------------------------------------------------
# Google Cloud Platform 项目配置
# -----------------------------------------------------------------------------
# PROJECT_ID: GCP 项目 ID，用于所有 Google Cloud 服务
# 获取方式: gcloud config get-value project
PROJECT_ID=your-gcp-project-id

# LOCATION: 部署区域，影响延迟和数据存储位置
# 推荐: us-central1 (美国中部) 或 asia-northeast1 (亚洲东北部)
LOCATION=us-central1

# FIRESTORE_DATABASE_ID: Firestore 数据库 ID
# 默认值: (default) - 使用项目的默认数据库
# 自定义: 如需使用特定数据库，请填入数据库 ID
FIRESTORE_DATABASE_ID=(default)

# -----------------------------------------------------------------------------
# Google Cloud Storage 配置
# -----------------------------------------------------------------------------
# GCS_BUCKET_NAME: 存储桶名称，用于存储用户上传的文件和生成的媒体
# 命名规则: 全局唯一，只能包含小写字母、数字、连字符
# 建议格式: project-name-assets 或 company-app-storage
GCS_BUCKET_NAME=your-bucket-name

# -----------------------------------------------------------------------------
# AI 模型配置
# -----------------------------------------------------------------------------
# MODEL: 使用的 AI 视频生成模型
# 可选值: 
#   - veo-3.0-generate-preview (最新预览版)
#   - veo-2.0-generate (稳定版)
# 注意: 不同模型可能有不同的配额和定价
MODEL=veo-3.0-generate-preview

# -----------------------------------------------------------------------------
# 应用安全配置
# -----------------------------------------------------------------------------
# APP_SECRET_KEY: 用户注册验证密钥，控制用户注册权限
# 生成命令: openssl rand -hex 12
# 用途: 用户注册时需要提供此密钥，防止恶意注册
# 长度: 建议 24 个字符 (12 字节的十六进制)
APP_SECRET_KEY=your-production-app-secret-key

# -----------------------------------------------------------------------------
# 可选配置
# -----------------------------------------------------------------------------
# LOG_LEVEL: 日志级别，控制应用输出的日志详细程度
# 可选值: error, warn, info, debug
# 生产环境推荐: info 或 warn
LOG_LEVEL=info

# USE_COSMO: 是否启用 Cosmo 功能 (实验性功能)
# 值: true 或 false
# 默认: false (生产环境建议关闭实验性功能)
USE_COSMO=false

# -----------------------------------------------------------------------------
# 服务账户配置 (通常不需要)
# -----------------------------------------------------------------------------
# GOOGLE_APPLICATION_CREDENTIALS: 服务账户密钥文件路径
# 注意: Cloud Run 环境中通常使用 IAM 角色，无需此配置
# 仅在本地开发或特殊部署环境中使用
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# =============================================================================
# 密钥生成命令汇总
# =============================================================================
# 生成 NextAuth 密钥:
# openssl rand -base64 32
#
# 生成应用注册密钥:
# openssl rand -hex 12
#
# 获取当前 GCP 项目 ID:
# gcloud config get-value project
#
# 列出可用区域:
# gcloud compute regions list
# =============================================================================